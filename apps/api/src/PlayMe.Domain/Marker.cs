namespace PlayMe.Domain;

/// <summary>
/// Sprint 0 placeholder so the Domain assembly compiles with at least one
/// public type. Replaced in Sprint 1 by real domain entities (Room, Match,
/// Move, Outcome) and per-game modules under Games/.
/// </summary>
public static class Marker
{
    public const string AssemblyName = "PlayMe.Domain";
}
