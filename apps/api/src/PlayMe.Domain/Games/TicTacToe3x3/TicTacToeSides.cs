namespace PlayMe.Domain.Games.TicTacToe3x3;

/// <summary>
/// Side identifiers for every Tic-Tac-Toe variant (3×3, 6×6, 9×9 — only
/// 3×3 is wired in Sprint 1). Lower-case per CLAUDE.md §2.3 #14.
/// </summary>
public static class TicTacToeSides
{
    public const string X = "x";
    public const string O = "o";
}
