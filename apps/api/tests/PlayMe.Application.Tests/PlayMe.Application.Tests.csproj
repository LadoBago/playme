<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <RootNamespace>PlayMe.Application.Tests</RootNamespace>
    <AssemblyName>PlayMe.Application.Tests</AssemblyName>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
    <!--
      Test code intentionally violates a few code-style analyzers that are
      strict in production code (xUnit + FluentAssertions idioms generate
      false positives for CA1707 underscores, CA2007 ConfigureAwait, etc.).
      Production code in src/ keeps TreatWarningsAsErrors via Directory.Build.props.
    -->
    <NoWarn>$(NoWarn);CA1707;CA2007;CA1062;CA1001</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\PlayMe.Application\PlayMe.Application.csproj" />
    <!--
      Sprint 2 PR #2 — tests `GraceMemberKey` (Infrastructure-side helper
      shared between the disconnect-grace scheduler and its sweeper).
      Application.Tests is the only test project in the solution; cross-
      project unit tests are fair game until that becomes friction.
    -->
    <ProjectReference Include="..\..\src\PlayMe.Infrastructure\PlayMe.Infrastructure.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="FluentAssertions" />
  </ItemGroup>
</Project>
